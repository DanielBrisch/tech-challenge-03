name: CI · targeting-service

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: DanielBrisch/tech-challenge-03/.github/workflows/reusable-ci-python.yml@main
    with:
      service: targeting-service
      ecr-repository: togglemaster/targeting-service
    secrets: inherit
